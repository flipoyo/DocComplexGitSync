% preamble.tex — shared macros, style, and title-page settings
\documentclass[11pt,a4paper]{report}

% Encoding and fonts
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}

% Geometry
\usepackage[margin=2.5cm]{geometry}

% Hyperlinks
\usepackage[hidelinks,colorlinks=false]{hyperref}

% Code listings
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  showstringspaces=false,
}

% TikZ for class diagrams and figures
\usepackage{tikz}
\usetikzlibrary{positioning,shapes.multipart,shapes.geometric,arrows.meta,fit,backgrounds}

% Todo notes (for marking stale sections)
\usepackage{todonotes}
\newcommand{\deprecated}[1]{\todo[color=orange!40]{DEPRECATED: #1}}

% Product macros — update these when the package name or version changes
\newcommand{\cgspkg}{\texttt{ComplexGitSync}}
\newcommand{\cgscli}{\texttt{cgitsync}}
\newcommand{\cgsversion}{0000.04}

% Title-page information
\title{%
  \cgspkg{} \cgsversion \\[0.5em]
  \large Documentation
}
\author{Nicolas Flipo}
\date{\today}
